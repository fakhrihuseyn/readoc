# Docs / Notes App

Simple local app to create, edit and save Markdown notes.

Usage

1. npm install
2. npm start
3. Open http://localhost:3000

Notes are saved to the `notes/` folder as `.md` files.
